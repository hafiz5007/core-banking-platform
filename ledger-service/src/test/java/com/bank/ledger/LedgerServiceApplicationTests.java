package com.bank.ledger;

import org.junit.jupiter.api.Test;

/** Smoke test: the Spring context loads against a real database. */
class LedgerServiceApplicationTests extends AbstractIntegrationTest {

  @Test
  void contextLoads() {
    // Fails if the application context cannot start.
  }
}
