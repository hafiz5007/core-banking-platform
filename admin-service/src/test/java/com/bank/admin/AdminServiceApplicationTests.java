package com.bank.admin;

import org.junit.jupiter.api.Test;

/** Smoke test: the Spring context loads against a real database. */
class AdminServiceApplicationTests extends AbstractIntegrationTest {

    @Test
    void contextLoads() {
        // Fails if the application context cannot start.
    }
}
